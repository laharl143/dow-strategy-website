/**
 * The only heroes with a known "special combo" (DOW-10) — a fixed pool the
 * user named, not a general-purpose tagging system. A hero outside this
 * list never shows the combo toggle at all, on the hero page or the board.
 */
export const COMBO_HERO_SLUGS = ['lycan', 'lifestealer', 'snapfire', 'io', 'underlord', 'morphling', 'centaur-warrunner'];
